doctype html
html(lang="ko")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        script(type="text/javascript" src="/js/users/edit.js")
        link(rel="stylesheet" href="/css/users/edit.css")
        title=pageTitle + " - 이메일 변경"
    body
        div(id="content")
            div(id="top")
                a(id="main_link" href="/")
                    h1 To Do
            h2(id="join_text") 이메일 변경
            div(id="main")
                div(class="row")
                    div(class="label_area")
                        label(for="current_email") 기존 이메일
                    div(class="input_area" id="current_email_input_area")
                        input(type="email" name="current_email" id="current_email")
                div(class="row")
                    div(class="label_area")
                        label(for="new_email") 새로운 이메일
                    div(class="input_area" id="new_email_input_area")
                        input(type="email" name="new_email" id="new_email" autocomplete=false)
                        button(id="send_code_btn") 코드 전송
                div(class="row" id="auth_code_row" style="display:none")
                    div(class="label_area")
                        label(for="auth_code") 인증 번호
                    div(class="input_area" id="auth_code_input_area")
                        input(type="text" name="auth_code" id="auth_code" maxlength=5 autocomplete=false)
                        button(id="auth_btn") 인증
                div(id="change_btn_area")
                    input(type="button" id="change_email_btn" value="변경하기")
                    input(type="button" id="back_btn" value="돌아가기")